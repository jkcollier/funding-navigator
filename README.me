# Funding Navigator

## Getting up and running

- `cp .env.example .env`
- Correct ports if necessary in this file
- `docker compose up -d`
- Wait a couple minutes until your images are all downloaded and built. 
- `docker compose exec web restore_database`
